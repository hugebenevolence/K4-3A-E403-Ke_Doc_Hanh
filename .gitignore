# Tài liệu đề bài BTC copy về để tiện đọc — không phải của repo nộp bài, không commit
# (bao gồm cả data/ bên trong — bảo mật dữ liệu, xem brief/README-goc-btc.md)
/brief/

# BTC data pack — never commit (phòng khi ai đó copy data/ ra ngoài brief/)
/data/

# secrets
.env
.env.*
!.env.example

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# node
node_modules/
dist/
.vite/

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
