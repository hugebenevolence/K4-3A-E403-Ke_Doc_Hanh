fastapi==0.141.1
uvicorn[standard]==0.53.0
websockets==16.1.1
pydantic==2.13.5
pydantic-settings==2.15.0

# Orchestration — workflow tất định (node + conditional edge), KHÔNG phải vòng lặp ReAct.
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1

# test
pytest==9.1.1
httpx==0.28.1

# LLM: gpt-5-nano (talker) / gpt-5-mini (chấm) / gpt-5 (judge offline)
openai==3.14.1

# Bóc slide PDF ra span + bbox
pymupdf==1.28.2

# Provider thật còn lại — bỏ comment khi viết adapter tương ứng.
# speechmatics-python  # STT realtime, có partial <500ms, hỗ trợ tiếng Việt
